name: Build FFmpeg (armeabi-v7a)

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/ffmpeg-build-reusable.yml
    with:
      termux_arch: arm
      android_abi: armeabi-v7a
    secrets: inherit
