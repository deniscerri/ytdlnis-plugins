name: Build Python (x86)

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/python-build-reusable.yml
    with:
      termux_arch: i686
      android_abi: x86
    secrets: inherit
